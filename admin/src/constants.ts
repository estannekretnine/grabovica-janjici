/** Podrazumevano stablo iz migracije (slug `default`). */
export const DEFAULT_TREE_ID = "00000000-0000-0000-0000-000000000001";

/** Podrazumevani unos u formi prijave (kolona email / naziv u tabeli korisnici). */
export const DEFAULT_LOGIN_EMAIL = "";

/** Podrazumevana lozinka u formi prijave (radi brzeg admin pristupa). */
export const DEFAULT_LOGIN_PASSWORD = "";
