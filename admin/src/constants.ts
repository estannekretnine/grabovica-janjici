/** Podrazumevano stablo iz migracije (slug `default`). */
export const DEFAULT_TREE_ID = "00000000-0000-0000-0000-000000000001";

/**
 * Fiksni demo-unos na strani prijave. Poziva signInAnonymously() da JWT bude
 * u ulozi authenticated (RLS). U Supabase: Authentication → Providers → Anonymous = ON.
 */
export const DEMO_USERNAME = "grabovica-janjici";
export const DEMO_PASSWORD = "1234567gr";
