{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix admin",
  "buildCommand": "npm run build --prefix admin",
  "outputDirectory": "admin/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
